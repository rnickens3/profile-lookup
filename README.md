# Profile Lookup
Function iterates through a list of contacts, check their properties, and output requested property

## User Stories
* You should create a function named lookUpProfile that takes a name and a property as arguments.
* You should retrieve contact information from the provided contacts array.
* If the function receives a contact name and the property exists on the related contact, then the property's value should be returned.
* If the name passed to the function does not match any contacts in the contacts array, then the function should return "No such contact".
* If the property does not exist on a found contact, then the function should return "No such property".

## Tools & Languages Used
* FreeCodeCamp.org
* JavaScript

## Features
* Returns property from requested name in contact list
* If property or name does not exist, gives error message

## Concepts Learned
* Iteration / Loops / Bracket Notation
* This assignment was very tough, and I learned a lot about the difference between bracket and dot notation. With the way this assignment works, bracket notation was the only way to accomplish what needed to be done!

## How to Run
1. Clone the repository
2. Run the script using Node.js or import the function into any JS file
